@using Bnan.Core.Extensions
@using Bnan.Ui.ViewModels.MAS

@model CompanyOwed_VM
@inject IViewLocalizer localizer
@inject IOptions<RequestLocalizationOptions> options
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
       ViewData["returnUrl"] = string.IsNullOrEmpty(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}"; 
    ViewData["Page-title"] = @localizer["CompanyOwed_Dues Data"];
    ViewData["Page-Hint"] = @localizer["CompanyOwed_Dues Hint"];
}

<style>
    body {
        overflow: hidden
    }

    @@media (max-width: 600px) {
        #basic-datatable1_wrapper .row:first-child > div {
            text-align: left;
            overflow: scroll;
            text-overflow: ellipsis;
        }

            #basic-datatable1_wrapper .row:first-child > div:last-child > * {
                text-align: right;
                overflow: hidden;
                text-overflow: ellipsis;
            }
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f9f9f9;
    }

    .table-hover tbody tr:hover {
        background-color: #f5f5f5;
        cursor: pointer;
    }

    /*     td,th {
                text-align: center;
            } */

    #_Nc {
        text-align: center;
    }

</style>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
@*<link href="http://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css" rel="stylesheet" />*@
<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>


@*<link href="~/BranchSys/Pages/css/mixed_style.css" rel="styleSheet" />*@


@if (requestCulture.RequestCulture.UICulture.Name == "en-US")
{
    @*<link href="~/BranchSys/Pages/css/English_style.css" rel="styleSheet" />*@
    <style>
        .modal-dialog {
            max-width: 610px;
            margin: 2rem auto;
            margin-left: 625px;
            margin-top: 140px;
        }

        .col-md-7 {
            width: 300px;
        }

        .col-md-5 {
            width: 200px;
        }

        .Direction_Page {
            direction: rtl;
        }

        .d-flex {
            gap: 15px;
            display: flex;
            -webkit-align-items: 0;
        }

        .mgCenter {
            margin-left: 185px;
        }

        #SalesPointLessorInformationSigntureImage {
            position: absolute;
            top: 39%;
            left: 50%;
        }

        .t_mar {
        @*margin: 35px;*@ margin-right: 20px;
        @*margin-top: 5px;*@
        }

        .col-md-label5 {
            /*width: 28.75%;*/
            width: 140px;
            height: 30px;
        }

        .col-md-input5 {
            width: 250px;
            margin-top: -8px;
            height: 30px;
        }

        .col-md-label10 {
            /*width: 28.75%;*/
            width: 80px;
            height: 30px;
        }

        .col-md-input10 {
            width: 310px;
            margin-top: -8px;
            height: 30px;
        }

        #Date_cusor2 {
            margin-right: 13px;
            direction: rtl;
        }
    </style>
}
else
{
    @*<link href="~/BranchSys/Pages/css/style.css" rel="styleSheet" />*@

    <style>
        .modal-dialog {
            max-width: 480px;
            margin: 2rem auto;
            margin-left: 625px;
            margin-top: 140px;
        }

        .col-md-7 {
            width: 270px;
        }

        .col-md-5 {
            width: 150px;
        }

        .Direction_Page {
            direction: ltr;
        }

        .d-flex {
            gap: 15px;
            display: flex;
            -webkit-align-items: 0;
        }

        .mgCenter {
            margin-right: 148px;
        }

        #SalesPointLessorInformationSigntureImage {
            position: absolute;
            top: 39%;
            right: 50%;
        }

        .t_mar {
        @*margin: 35px;*@ margin-left: 20px;
        @*margin-top: 5px;*@
        }

        .col-md-label5 {
            /*width: 28.75%;*/
            width: 90.5px;
            height: 30px;
        }

        .col-md-input5 {
            width: 300px;
            margin-top: -8px;
            height: 30px;
        }

        .col-md-label10 {
            /*width: 28.75%;*/
            width: 75px;
            height: 30px;
        }

        .col-md-input10 {
            width: 315px;
            margin-top: -8px;
            height: 30px;
        }

        #Date_cusor2 {
            margin-left: 13px;
            direction:ltr;
        }
    </style>
}

<div class="sub-container">
    <div class="container-fluid">

        <div class="" id="MAS" style="height:540px;width:1667px;margin-inline-start:10px;">
            @*<form class="needs-validation" novalidate asp-area="MAS" asp-controller="CompanyOwed" asp-action="AddPaymentTaxValues" method="post">*@
            <div  >
                <div style="height:500px">
                    @*Document Code*@
                    <div class="row mt-2" hidden>
                        <div class="col-md-4">
                            @*<input type="text" class="form-control" readonly asp-for="CrMasUserInformationLessor" />
                            <input type="text" class="form-control" readonly asp-for="CrMasUserInformationDefaultBranch" />
                            <input type="text" class="form-control" readonly asp-for="CrMasUserInformationCode" />*@
                        </div>
                    </div>
                    @*Document Branch*@
                    <div class="row mt-2">
                        @if (requestCulture?.RequestCulture.UICulture.Name == "ar-EG")
                        {
                            <div class="col-md-1 formlabel" style="margin-top:7px;width:185px">
                                <label class="form-label">@localizer["CompanyOwed_pay_pay_Tax_no"]</label>
                            </div>
                        }
                        else
                        {
                            <div class="col-md-1 formlabel" style="margin-top:6px;width:160px">
                                <label class="form-label">@localizer["CompanyOwed_pay_pay_Tax_no"]</label>
                            </div>
                        }
                        <div class="col-md-2 col-11">
                            <input type="text" class="form-control" id="New_CompanyOwed_Tax_no" name="New_CompanyOwed_Tax_no" value="-" readonly />
                            @*@if (requestCulture?.RequestCulture.UICulture.Name == "ar-EG")
                            {
                                <input type="text" class="form-control" asp-for="CrMasUserInformationArName" readonly/>
                            }
                            else
                            {
                                <input type="text" class="form-control" asp-for="CrMasUserInformationEnName" readonly/>
                            }*@
                        </div>
                    </div>

                    <div style="display:flex;gap:10px;justify-content:space-between;">

                        @if (requestCulture.RequestCulture.UICulture.Name == "en-US")
                        {
                            <div class="" style="display:flex;gap:77px;margin-inline-start:2px" id="Second_row">

                                <div class="Branch" style="display:flex;gap:10px;">
                                    <label for="brunch_name" style="padding-top:8px;width:70px;margin-top:4px;margin-inline-start:-2px">@localizer["CompanyDues_Company_Name"]</label>
                                    @*<select class="text-right form-select" id="SelectedType" onchange="getSelectedType(this)"  style="width:150px;border-color:#74D9EB;border-radius:5px;">*@


                                    @*<select class="text-right form-select" id="SelectedCompany" onchange="getSelectedType(this)" style="width:150px;">*@
                                    <select class="text-right form-select" id="SelectedCompany" onchange="getSelectedCompany(this)" style="width:150px;margin-inline-start:49px">
                                        @*<option id="1" selected="selected" value="1">@localizer["CarStatistics_Branches"]</option>*@
                                        @foreach (var One in Model.CrMasLessorInformation)
                                        {
                                            <option id="@One.CrMasLessorInformationCode" value="@One.CrMasLessorInformationCode">@One.CrMasLessorInformationEnShortName</option>
                                        }
                                    </select>


                                </div>

                                <div style="display:flex; gap : 7px;">
                                    <label class="form-label p-0" style="margin-top:11px;margin-inline-start:12px">@localizer["CompanyDues_Company_ContractNo"]</label>
                                    <select class="text-right form-select" id="SelectedContract" onchange="getSelectedContract(this)" onload="getSelectedContract(this)" style="width:230px;">
                                        @*<option id="1" selected="selected" value="1">@localizer["CarStatistics_Branches"]</option>*@
                                        @foreach (var single in Model.Drop_Conracts_List)
                                        {
                                            <option id="@single" value="@single">@single</option>
                                        }
                                    </select>
                                </div>

                                <div style="display:flex; gap : 7px;margin-inline-start:-40px;">
                                    @*<label class="form-label p-0" style="margin-top:11px;">@localizer["CompanyDues_Contract_Type"]</label>*@
                                    <input type="text" class="form-control p-0" id="Company_Contract_Type" name="Company_Contract_Type" value="" readonly style="margin-top:3px;" />
                                </div>

                                <div style="width:50px;margin-inline-start:-150px;">
                                    <a class="btn btn-primary" id="Button_DateFilter" name="Button_DateFilter">
                                        <i class="mdi mdi-search-web"></i>
                                    </a>
                                </div>
                            </div> @*EndRow*@
                        }
                        else
                        {
                            <div class="" style="display:flex;gap:77px;margin-inline-start:2px" id="Second_row">

                                <div class="Branch" style="display:flex;gap:10px;">
                                    <label for="brunch_name" style="padding-top:8px;width:70px;margin-top:4px;margin-inline-start:-2px">@localizer["CompanyDues_Company_Name"]</label>
                                    @*<select class="text-right form-select" id="SelectedType" onchange="getSelectedType(this)"  style="width:150px;border-color:#74D9EB;border-radius:5px;">*@


                                    @*<select class="text-right form-select" id="SelectedCompany" onchange="getSelectedType(this)" style="width:150px;">*@
                                    <select class="text-right form-select" id="SelectedCompany" onchange="getSelectedCompany(this)" style="width:150px;margin-inline-start:50px">
                                        @*<option id="1" selected="selected" value="1">@localizer["CarStatistics_Branches"]</option>*@
                                        @foreach (var One in Model.CrMasLessorInformation)
                                        {
                                            <option id="@One.CrMasLessorInformationCode" value="@One.CrMasLessorInformationCode">@One.CrMasLessorInformationArShortName</option>
                                        }
                                    </select>

                                </div>

                                <div style="display:flex; gap : 7px;">
                                    <label class="form-label p-0" style="margin-top:11px;margin-inline-start:42px">@localizer["CompanyDues_Company_ContractNo"]</label>
                                    <select class="text-right form-select" id="SelectedContract" onchange="getSelectedContract(this)" onload="getSelectedContract(this)" style="width:230px;">
                                        @*<option id="1" selected="selected" value="1">@localizer["CarStatistics_Branches"]</option>*@
                                        @foreach (var single in Model.Drop_Conracts_List)
                                        {
                                            <option id="@single" value="@single">@single</option>
                                        }
                                    </select>
                                </div>

                                <div style="display:flex; gap : 7px;margin-inline-start:-30px;">
                                    @*<label class="form-label p-0" style="margin-top:11px;">@localizer["CompanyDues_Contract_Type"]</label>*@
                                    <input type="text" class="form-control p-0" id="Company_Contract_Type" name="Company_Contract_Type" value="" readonly style="margin-top:3px;" />
                                </div>

                                <div style="width:50px;margin-inline-start:-190px;">
                                    <a class="btn btn-primary" id="Button_DateFilter" name="Button_DateFilter">
                                        <i class="mdi mdi-search-web"></i>
                                    </a>
                                </div>
                            </div> @*Half EndRow*@
                        }
                        @*<div style="-webkit-flex-align:start;display:flex;gap:10px" class="mt-3">


                            <div style="display:flex; gap : 7px;">
                                <input type="radio" id="customRadioAll" name="customRadio" value="All" class="form-check-input p-0">
                                <label class="p-0" for="customRadioAll">@localizer["All"]</label>
                            </div>
                            <div style="display:flex; gap : 7px;">
                                <input type="radio" id="status_Is_Paid" name="customRadio" value="1" class="form-check-input">
                                <label class="p-0" for="status_Is_Paid">@localizer["status_Is_Paid"]</label>
                            </div>
                            <div style="display:flex; gap : 7px;">
                                <input type="radio" id="status_Not_Paid" name="customRadio" value="0" class="form-check-input">
                                <label class="p-0" for="status_Not_Paid">@localizer["status_Not_Paid"]</label>
                            </div>

                        </div>*@
                    </div> @*EndRow*@

                    @*DateTime Inputs*@
                    <div class="row mt-2" id="DateInputs" style="display:flex;gap: 15px;-webkit-flex-item-align:center;height:45px;">
                        <div class="col-md-1 formlabel" style="margin-top:7px;width:113px">
                            <label class="form-label">@localizer["CompanyOwed_pay_Date"]</label>
                        </div>

                        <div class="dateRelative col-md-5 col-11" style="display:grid;width:220px;height:40px;">
                            <input type="text" class="form-control " id="endDateInput" name="endDateInput" style="grid-column: 1;grid-row: 1;width:220px;" />
                            <i class="dripicons-calendar" id="Date_cusor2" style="cursor:pointer; grid-column: 1;grid-row: 1;  margin-top:10px; "></i>
                        </div>
                        @*<div style="width:50px;">

                            <a class="btn btn-primary" id="GetAllCheckBox_Checked" onclick="GetAllCheckBox_Checked()">
                                <i class="mdi mdi-search-web"></i>
                            </a>
                        </div>*@
                    </div> @*End row*@
                    @*Value Offer*@
                    <div class="row mt-2">
                        <div class="col-md-1 formlabel" style="margin-top:6px;width:115px">
                            <label class="form-label">@localizer["CompanyOwed_pay_pay_Tax_Total_Value"]</label>
                        </div>
                        <div class="col-md-2 col-8">
                            <input type="number" class="form-control" id="Amount_will_Pay" name="Amount_will_Pay" readonly value="@Model.Amount_will_Pay">
                        </div>
                    </div>
                    @*Reasons*@
                    <div class="row mt-2">
                        <div class="col-md-1 formlabel" style="margin-top:7px;width:125px">
                            <label class="form-label">@localizer["reasons"]</label>
                        </div>
                        <div class="col-md-5 col-11">
                            <input type="text" class="form-control" id="Reasons" name="Reasons" autocomplete="off" maxlength="100" style="width:500px;">
                        </div>
                    </div>

                    <div class="dataTable " style="height:240px;margin-top:15px;margin-inline-end:12px;">
                        <partial name="_DataTablePaymentTaxValues" />
                    </div>
                </div>
                <div style="margin-top:35px">
                    @*<button class='btn btn-primary' type="submit" >@localizer["Save"]</button>*@
                    <button class='btn btn-primary' onclick="GetAllCheckBox_Checked()">@localizer["Save"]</button>
                    <a type="button" class="btn btn-danger ms-2" title="@localizer["Back"]" asp-area="MAS" asp-controller="CompanyOwed" asp-action="Index">
                        @localizer["Back"]
                    </a>
                </div>

            </div>
        </div>

    </div>
</div>




<style>
    .wrapper {
        margin-bottom: 60px;
        overflow: hidden;
        background-image: url("~/Pictures/bg.jpg") !important;
    }

    .table-bordered td,
    .table-bordered th {
        border: none;
    }
</style>


@section scripts {


    @*Get AccountBank Using filter*@

    <script>
        $(document).ready(function () {
            var today = new Date();
            var startDate = new Date();
            var EndDate = new Date();
            startDate.setDate(today.getDate() - 30);


            var startDates = $("startDateInput").val();
            var StartformattedDate = new Date(startDates);

            var EndDates = $("endDateInput").val();
            var EndformattedDate = new Date(EndDates);


            if (!StartformattedDate || StartformattedDate === "") {
                StartformattedDate = today; // Set date to today if it's empty or null
            }
            if (!EndformattedDate || EndformattedDate === "") {
                EndformattedDate = today; // Set date to today if it's empty or null
            }




            $("#startDateInput").datepicker({
                dateFormat: 'yy/mm/dd',
                defaultDate: startDate, // Set the minimum date to today
                maxDate: today,
            }).datepicker("widget").addClass("datepicker-widget");
            $("#startDateInput").datepicker("setDate", startDate);
            $("#startDateInput").click(function () {
                $(this).datepicker("show");
            });

            $("#endDateInput").datepicker({
                dateFormat: 'yy/mm/dd',
                defaultDate: today,// Set the minimum date to today
                maxDate: today,
            }).datepicker("widget").addClass("datepicker-widget");
            $("#endDateInput").datepicker("setDate", today);
            $("#endDateInput").click(function () {
                $(this).datepicker("show");
            });

            $("#Date_cusor1").click(function () {
                $(this).prevAll('input').datepicker("show");
            });

            $("#Date_cusor2").click(function () {
                $(this).prevAll('input').datepicker("show");
            });

        });
    </script>

    <script>
        function GetAllCheckBox_Checked() {
            yourArray = [];
            // return list of All CheckBoxes Has Checked
            $("input:checkbox[name=Contracts_checked]:checked").each(function () {
                yourArray.push($(this).val());              
            });
            console.log(yourArray);
            var pay_date = document.getElementById("endDateInput").value;
            var reasons = document.getElementById("Reasons").value;
            var Total_Money = document.getElementById("Amount_will_Pay").value;
            var Serial_pay = document.getElementById("New_CompanyOwed_Tax_no").value;
            var Company = document.getElementById("SelectedCompany").value;
            var company_ContractNo = document.getElementById("SelectedContract").value;
            var postData = { values: yourArray, pay_date: pay_date, reasons: reasons, Total_Money: Total_Money, Serial_pay: Serial_pay, Company: Company, company_ContractNo: company_ContractNo };

            //var array = []
            //var checkboxes = document.querySelectorAll('input[type=checkbox]:checked')
            //// return list of All CheckBoxes Has Checked
            //for (var i = 0; i < checkboxes.length; i++) {
            //    array.push(checkboxes[i].value);
            //}
            //console.log(array);
            //var postData = { values: yourArray };
            $.ajax({
                type: "Post",
                data: postData,
                url: "@Url.Action("AddPaymentTaxValues", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    console.log(response);           
                    if (response.code === 1) {
                        //window.location = 'yourloginpage.html'
                        console.log("response success");
                        location.href = '@Url.Action("SuccessToast", "CompanyOwed")?';
                    }
                    else
                    {
                        console.log("response Failed As No");
                    }
                    //$("#FromAccountNo").val(response.accountIban);
                    //@*if ("@requestCulture?.RequestCulture.UICulture.Name" == "ar-EG") {*@
                    //    $("#FromBank").val(response.bankNo);
                    //    $("#FromBankk").val(response.arBank);
                    //}
                    //else {
                    //    $("#FromBank").val(response.bankNo);
                    //    $("#FromBankk").val(response.arBank);

                    //}
                },
                //error: function (response) {
                //    console.log("response Error");
                //    location.href = '@Url.Action("FailerToast", "CompanyOwed")?';
                //},
            })
        };
    </script>
    
    <script>
        //get AccountBank By Status function
        function GetReportFitlerDate(status) {
            var id = $("#FTR_Id").val();
            var startDate = $("#startDateInput").val();
            var endDate = $("#endDateInput").val();

            console.log(startDate)
            console.log(endDate)
            if (status === "All") {
                const yesBtn = document.getElementById('customRadioAll');
                // ✅ Set the radio button to checked
                yesBtn.checked = true;
            }

            // Clear the value from the search input
            $("#top-search").val("");

            $.ajax({
                type: "GET",
                data: {
                    _max: endDate,
                    _mini: startDate,
                    status: status,
                },
                url: "@Url.Action("GetAllContractsByDate_status", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    $(".dataTable").html(response);
                    set_basic_datatable();
                },

            })
        }


        
    </script>
@*
    <script>
        function AddPaymentTaxValues_AfterSelectComp() {
            var Company = document.getElementById("SelectedCompany").value;
            var Contract = document.getElementById("SelectedContract").value;
            console.log(Company);
            console.log(Contract);
            //console.log(Company, Contract);
            console.log({ Company, Contract });

            var startDate = $("#startDateInput").val();
            var endDate = $("#endDateInput").val();

            console.log(startDate)
            console.log(endDate)
            if (status === "All") {
                const yesBtn = document.getElementById('customRadioAll');
                // ✅ Set the radio button to checked
                yesBtn.checked = true;
            }

            // Clear the value from the search input
            $("#top-search").val("");

            $.ajax({
                type: "GET",
                data: {
                    _max: endDate,
                    _mini: startDate,
                    status: status,
                    Company: Company,
                    Contract: Contract,
                },
                url: "@Url.Action("GetAllContractsByDate_status", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    $(".dataTable").html(response);
                    set_basic_datatable();
                    //document.getElementById('Company_ContractNo').Value = '@ViewBag.Company_ContractNo';
                    //var xx = '@ViewBag.Company_ContractNo';
                    //console.log(xx);
                    //var yy = '@ViewBag.EndDate';
                    //console.log(yy);

                },
            })
        }

    </script>*@

    <script>
        function AddToAmount(x){
            let Amount_will_Pay = document.getElementById("Amount_will_Pay");
            //let num2 = parseFloat("10.547892").toFixed(2)
            let Amount_will = parseFloat(Amount_will_Pay.value) + parseFloat(x);
            return Amount_will.toFixed(2);
        };
        function MinusFromAmount(x) {
            let Amount_will_Pay = document.getElementById("Amount_will_Pay");
            //let num2 = parseFloat("10.547892").toFixed(2)
            let Amount_will = parseFloat(Amount_will_Pay.value) - parseFloat(x);
            return Amount_will.toFixed(2);

        };

        function CheckBoxClicked(e) {
            let Amount_will_Pay = document.getElementById("Amount_will_Pay");
            if (e.checked) {
                var values = e.id;
                let newVal = AddToAmount(values);
                Amount_will_Pay.value = newVal
                console.log(newVal);
            } 
            else {
                var values = e.id;
                let newVal = MinusFromAmount(values);
                Amount_will_Pay.value = newVal
                console.log("No Value");
            }
        };
    </script>


    <script>
        //get AccountBank By Status function
        function GetAllContracts_ToDataTable() {
            var Company = document.getElementById("SelectedCompany").value;
            var company_ContractNo = document.getElementById("SelectedContract").value;
            console.log(Company);
            console.log(company_ContractNo);
            //console.log(Company, Contract);
            console.log({ Company, company_ContractNo });

            $.ajax({
                type: "GET",
                data: {
                    Company: Company,
                    company_ContractNo: company_ContractNo,
                },
                url: "@Url.Action("GetAllContracts_ToDataTable", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    $(".dataTable").html(response);
                    set_basic_datatable();
                    //document.getElementById('Company_ContractNo').Value = '@ViewBag.Company_ContractNo';
                    //var xx = '@ViewBag.Company_ContractNo';
                    //console.log(xx);
                    //var yy = '@ViewBag.EndDate';
                    //console.log(yy);

                },
            })
        }
    </script>

    <script>
        //get AccountBank By Status function
        function GetReportFitlerDate(status) {
            var Company = document.getElementById("SelectedCompany").value;
            var Contract = document.getElementById("SelectedContract").value;
            console.log(Company);
            console.log(Contract);
            //console.log(Company, Contract);
            console.log({ Company, Contract });

            var startDate = $("#startDateInput").val();
            var endDate = $("#endDateInput").val();

            console.log(startDate)
            console.log(endDate)
            if (status === "All") {
                const yesBtn = document.getElementById('customRadioAll');
                // ✅ Set the radio button to checked
                yesBtn.checked = true;
            }

            // Clear the value from the search input
            $("#top-search").val("");

            $.ajax({
                type: "GET",
                data: {
                    _max: endDate,
                    _mini: startDate,
                    status: status,
                    Company: Company,
                    Contract: Contract,
                },
                url: "@Url.Action("GetAllContractsByDate_status", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    $(".dataTable").html(response);
                    set_basic_datatable();
                    //document.getElementById('Company_ContractNo').Value = '@ViewBag.Company_ContractNo';
                    //var xx = '@ViewBag.Company_ContractNo';
                    //console.log(xx);
                    //var yy = '@ViewBag.EndDate';
                    //console.log(yy);

                },
            })
        }
        //on Click Filter Radio Button GetLessorsByStatus
        $("input[name='customRadio']").on("click", function () {
            // Clear the value from the search input
            $("#top-search").val("");
            //GetRenterContractFitler($(this).val())
            GetReportFitlerDate($(this).val());
        });

        //on Click Filter Radio Button GetLessorsByStatus
        $("a[name='Button_DateFilter']").on("click", function () {
            // Clear the value from the search input
            //$("#top-search").val("");
            //GetRenterContractFitler($(this).val())
            //GetReportFitlerDate("All");
            GetAllContracts_ToDataTable();
        });


    </script>

    <script>
        //get AccountBank By Status function
        function GetAllContractsByCom(comp) {
            var Company = document.getElementById("SelectedCompany").value;
            console.log(Company);
            //var startDate = $("#startDateInput").val();
            var endDate = $("#endDateInput").val();

            //console.log(startDate)
            console.log(endDate)

            $.ajax({
                type: "GET",
                data: {
                    Company: Company,
                },
                url: "@Url.Action("GetAllContractsByCom", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    console.log("DDDone");
                    console.log(response.contracts)
                    $("#SelectedContract").find("option").remove().end()
                    for (var i = 0; i < response.contracts.length; i++) {
                        var select = document.getElementById("SelectedContract");
                        var option = document.createElement("option");
                        //option.text = titelliste[i].Titel;
                        //option.value = titelliste[i].Produktid;

                        option.text = response.contracts[i];
                        option.value = response.contracts[i];
                        //if (i === 0) {
                        //    option.selected = 'selected';
                        //}
                        select.add(option);
                    }
                    var type2 = "";
                    console.log(response.contract_1_Type);
                    switch (response.contract_1_Type) {
                        case "1":
                            type2 = '@localizer["CompanyDues_TypeOfContract_Sub"]';
                            break;
                        case "2":
                            type2 = '@localizer["CompanyDues_TypeOfContract_value"]';
                            break;
                        case "3":
                            type2 = '@localizer["CompanyDues_TypeOfContract_percenteage"]';
                            break;
                        default:
                            type2 = '@localizer["CompanyDues_TypeOfContract_Sub"]';
                    }
                    document.getElementById("Company_Contract_Type").value = type2;
                    console.log(response.payNo_New)
                    document.getElementById("New_CompanyOwed_Tax_no").value = response.payNo_New;
                    

                },
            })
        }
    </script>


    <script>
        //get AccountBank By Status function
        function GetNameTypeOfContract(Contract) {

            //const yesBtn = document.getElementById('customRadioAll');
            //// ✅ Set the radio button to checked
            //yesBtn.checked = true;


            //// Clear the value from the search input
            //$("#top-search").val("");

            $.ajax({
                type: "GET",
                data: {
                    Contract: Contract,
                },
                url: "@Url.Action("GetNameTypeOfContract", "CompanyOwed", new { Area = "MAS" })",
                success: function (response) {
                    console.log("DDDone");
                    console.log(response.contract_type)
                    var type = "";
                    switch (response.contract_type) {
                        case "1":
                            type = '@localizer["CompanyDues_TypeOfContract_Sub"]';
                            break;
                        case "2":
                            type = '@localizer["CompanyDues_TypeOfContract_value"]';
                            break;
                        case "3":
                            type = '@localizer["CompanyDues_TypeOfContract_percenteage"]';
                            break;
                        default:
                            type = '@localizer["CompanyDues_TypeOfContract_Sub"]';
                    }
                    document.getElementById("Company_Contract_Type").value = type;

                },
            })
        }
    </script>

    <script>
        //onchange="getSelectedCompany(this)"
        function getSelectedCompany(selectObject) {
            var single = selectObject.value;
            //var type = "";
            //var listDrop = "@ViewBag.concate_DropDown";
            //console.log(single);

            //var startDate = $("#startDateInput").val();
            //var endDate = $("#endDateInput").val();

            //console.log(startDate)
            //console.log(endDate)

            GetAllContractsByCom(single);
        };
    </script>

    <script>
        //onchange="getSelectedContract(this)"
        function getSelectedContract(selectObject) {
            var single = selectObject.value;


            GetNameTypeOfContract(single);
        };
    </script>
}
